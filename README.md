# daily-learning-notes

Daily learning logs generated from Hermes chat history.

Each file in `docs/daily/` corresponds to one day (DD-MM-YYYY) and is
auto-committed by the `daily-learning-log` cron job.
