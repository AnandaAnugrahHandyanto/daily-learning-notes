# Learning Log — 05-09-2026

## Summary
First run of the `daily-learning-log` cron job itself. Setup day: configured the
recurring 20:00 WIB job, extended `SOUL.md` with a Senior DevOps role, surfaced
a leaked GitHub PAT in chat, and pulled the last 5 commits from
`AnandaAnugrahHandyanto/PertanianHub` via the GitHub MCP server.

## Topics Discussed
- Recurring cron scheduling in Hermes (`cronjob_manage`) and timezone handling
  (UTC cron field vs `Asia/Jakarta` user expectation).
- Personality / role updates for `Savarez` in `~/.hermes/SOUL.md` — added
  "Senior DevOps (GitHub Repo Manager)" and CI/CD + repo management expertise.
- Security: a GitHub PAT (`ghp_...`) was pasted in plaintext to Telegram — was
  flagged for immediate revoke and warned against re-pasting secrets.
- GitHub MCP usage: `search_repositories` + `list_commits` to inspect an
  external repo (`AnandaAnugrahHandyanto/PertanianHub`).
- Schema validation lessons: `tool_call`/`cronjob_manage` reject `enabled_toolsets`
  as a wrapped object and reject extra fields like `reason` inside `arguments`.

## Key Decisions
- Cron schedule finalized to `0 13 * * *` (UTC) = 20:00 WIB. Natural-language
  `every day at 8pm` is ambiguous between UTC and local, so we pin to the cron
  expression explicitly.
- Cron job `daily-learning-log` (id `226902f65fb0`) is the first fire of the new
  pipeline; this log is its smoke test.
- Repo `daily-learning-notes` is bootstrapped locally first, then pushed to
  GitHub via `mcp__GITHUB__create_repository` to avoid double-init conflicts.
- SOUL.md gains DevOps + CI/CD responsibilities to match the new recurring
  repo-management tasks Master is delegating.

## Code / Commands
- `git init -b main` in `~/daily-learning-notes`; structure:
  - `README.md`
  - `docs/.gitkeep`
  - `docs/daily/DD-MM-YYYY.md`
- `cronjob_manage create` with `schedule="0 13 * * *"`, `deliver="origin"`,
  `failure_deliver="origin"`, `workdir="/home/savarez"`, `name="daily-learning-log"`.
- `mcp__GITHUB__search_repositories` (query="PertanianHub") →
  `AnandaAnugrahHandyanto/PertanianHub`.
- `mcp__GITHUB__list_commits` (owner, repo, perPage=5) →
  1. `1a22e6f` chore: make dependencies compatible with PHP 8.3
  2. `34bad7c` test: configure mysql test environment
  3. `af782ef` fix: polish responsive dashboard and profile UI
  4. `53f8b9e` style: update UI colors for sales forms and financial reports
  5. `2e10005` fix: polish login submit button
- `git -c user.name="Savarez" -c user.email="savarez@users.noreply.github.com"
   commit -m "chore(daily): add learning log for 05-09-2026"`

## Open Questions / Follow-ups
- PAT exposed in chat: confirm Master has revoked it and stored the new one
  via `gh auth login` or a credential helper — needed for `git push` to
  succeed without falling back to MCP `push_files`.
- Cron job parameter schema: `enabled_toolsets` must be a flat array of
  strings, not `{item: [...]}`. Worth documenting in a skill so future
  job creation does not repeat the same schema mistake.
- Decide whether `gh` CLI should be installed on this host (`gh: command not
  found`) or stay MCP-only for GitHub ops.

## Tomorrow
- Run #2 of `daily-learning-log` at 20:00 WIB; pipeline should be fully
  unattended.
- Verify push works end-to-end with PAT (or MCP fallback) and capture the
  commit SHA in the daily report.
