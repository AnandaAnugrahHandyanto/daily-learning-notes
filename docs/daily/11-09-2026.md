# Learning Log — 11-09-2026

## Summary
Finalized Tryout Online (AkademikPro) release v1.0.0 on 2026-09-11. Focus was cache/DB stabilization, smoke verification across 4 roles, backup generation, and changelog plus annotated tag creation. Restored production DB from backup after empty state and confirmed all suites green before committing.

## Topics Discussed
- Tryout Online Fase 8D final testing and release preparation (Laravel 11.6.1, PHP 8.4, MySQL/MariaDB, Breeze, Tailwind, Vite, Chart.js)
- Cache strategy: `optimize` vs `optimize:clear` + `config:clear` for dev/test (cached config caused 419 and 14 failed tests)
- Database restore from `backup/tryout_online_v1.0.sql` (31K, 14 INSERT, 20 CREATE TABLE) — 5 users / 2 tryouts / 5 soal / 1 hasil / 2 notifikasi
- Smoke tests via artisan serve on 127.0.0.1:8001 — login for admin/guru/siswa/orang_tua, siswa tryout list/exam timer, parent dashboards, notifications
- Changelog authoring and git tagging workflow for v1.0.0

## Key Decisions
- Use `php artisan optimize:clear` + `config:clear` as final dev state instead of `optimize` — prevents session/APP_KEY mismatch causing 419 and test failures; verified via `25 passed (60 assertions)` in 2.87s.
- Created second tryout `Tryout Smoke v1.0` for fresh exam smoke with adik user (kayla.adik@tryout.test) after Kayla already submitted tryout 1 (302 → hasil block is valid); used `syncWithoutDetaching` for 5 soal.
- Regenerated backup to 31K after adding second tryout and adik data, then restored when DB was empty (users 0) back to 5/2/5/1/2.
- Moved annotated tag `v1.0.0` from `3964f8d` to `f7849d8` with message `Release v1.0.0 - Sistem Akademik Tryout Online`; push deferred pending remote setup.

## Code / Commands
- `php artisan optimize:clear; php artisan cache:clear; php artisan config:clear; php artisan route:clear; php artisan view:clear; php artisan optimize` then `optimize:clear` + `config:clear` for testing
- `mysql -u root -p"$DBP" -e "SELECT 'users',COUNT(*) FROM tryout_online.users UNION ALL SELECT 'tryout',COUNT(*) FROM tryout_online.tryout ..."`
- `mysqldump -u root -p"$DBP" tryout_online > backup/tryout_online_v1.0.sql` (14 INSERT)
- `php artisan tinker --execute "Tryout::firstOrCreate(['nama'=>'Tryout Smoke v1.0'], [...])->soal()->syncWithoutDetaching(...)"`
- `curl -s -c /tmp/jar -b /tmp/jar http://127.0.0.1:8001/login | grep -oP 'name="_token" value="\K[^"]+'` + `curl -X POST /login -d "_token=...&email=...&password=password&role=..."` (4 roles 302→200 PASS)
- `curl -X POST /siswa/tryout/2/start -d "_token=..."` → `302 -> /siswa/tryout/2/exam 200`, timer `remaining=3600`, `timerEl` + `fetch save/ragu/submit` verified
- `git add CHANGELOG.md backup/tryout_online_v1.0.sql; git commit -m "chore(fase-8d): final testing + tag v1.0.0 + changelog"` (f7849d8, 2 files, 795 insertions); `git tag -a v1.0.0 -f -m "Release v1.0.0 - Sistem Akademik Tryout Online"`

## Open Questions / Follow-ups
- Remote not configured (`git remote -v` empty) — need `git remote add origin <url> && git push -u origin main && git push origin v1.0.0 --force`
- Parent selector multi-child (`?siswa_id=`) and Chart.js 5-last-scores need manual UI re-check on mobile 390px
- PWA installability (manifest + sw.js `akademikpro-v1`) not smoke-tested in this run

## Tomorrow
- Set git remote and push main + v1.0.0 tag to GitHub
- Manual browser QA for exam autosave/keepalive (30s ping + beforeunload) and auto-submit on timeout
- Continue daily log streak
