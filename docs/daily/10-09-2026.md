# Learning Log — 10-09-2026

## Summary
Worked on two main threads today: designing a WhatsApp bot that only responds when tagged in groups (ignoring private chats) using Baileys, and preparing Q&A material for the tahfidz/hafalan monitoring thesis system proposed to Pak Ilham. Also handled brief casual interactions on WhatsApp.

## Topics Discussed
- WhatsApp bot: tag-only in groups, ignore private chats — Baileys setup with `isGroup` filter and mention detection.
- Hafalan Tahfidz system (Alip's questions): web-based vs mobile, real-time parent transparency, and user roles (Admin, Ustadz/Musyrif, Orang tua, Santri).
- Development methodology and tech stack: Waterfall vs Agile/Prototype, Laravel + MySQL + Blade + Tailwind CSS + Vite, VPS/Nginx/Docker deployment.
- Casual WhatsApp exchanges and scheduling fragment with Pak Ilham.

## Key Decisions
- Use **@whiskeysockets/baileys** for the tag-only bot — lightest path, no Cloud API verification, filter via `remoteJid.endsWith('@g.us')` and mention check; private JIDs return early.
- Hafalan system as **responsive web monolith** (skip React/Vue/Flutter) — YAGNI; add PWA later if mobile-like UX is needed.
- Recommend **Waterfall** methodology for thesis documentation completeness, with Agile/Prototype noted as alternative if iterative mentoring is required.
- Keep surface area minimal: visual-only toggles and group-scoped commands over separate settings screens.

## Code / Commands
- `mkdir wa-tag-bot && cd wa-tag-bot && npm init -y && npm i @whiskeysockets/baileys qrcode-terminal pino`
- Baileys snippet: `const isGroup = remoteJid.endsWith('@g.us'); if (!isGroup) return;` + `mentionedJidList` / `contextInfo.mentionedJid` check for tag-only reply.
- Stack recommendation: `Laravel / MySQL / Blade + Tailwind CSS (mobile-first) / Vite / Nginx + Docker on VPS`
- `TZ=Asia/Jakarta date +%d-%m-%Y` → `10-09-2026`

## Open Questions / Follow-ups
- Finalize Baileys mention detection: confirm `botJid` vs `sock.user.id` format and `contextInfo` parsing for tag trigger.
- Pak Ilham appointment details incomplete — need date, time, and location.
- Draft formal paragraphs for thesis Chapter 3 (methodology + technology) from the Q&A summaries.

## Tomorrow
- Prototype the `wa-tag-bot` group-tag handler with QR auth and a test group.
- Prepare formal proposal text for the hafalan system (Bab 3) if requested.
- Continue daily learning log streak.
